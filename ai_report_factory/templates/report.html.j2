
<!doctype html><html><head><meta charset="utf-8"><title>AI Report Factory</title>
<style>body{font-family:Arial,Helvetica,sans-serif;margin:32px;color:#222}h1,h2{margin:.6em 0 .4em}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.card{border:1px solid #ddd;border-radius:8px;padding:12px}img{max-width:100%;height:auto;border:1px solid #eee;border-radius:6px}</style>
</head><body>
<h1>Monthly Commerce Performance Report</h1>
<div class="grid">
  <div class="card"><b>Revenue</b><br>${{ '{:,.0f}'.format(kpis.revenue) }}</div>
  <div class="card"><b>Profit</b><br>${{ '{:,.0f}'.format(kpis.profit) }}</div>
  <div class="card"><b>Orders</b><br>{{ '{:,}'.format(kpis.orders) }}</div>
  <div class="card"><b>Customers</b><br>{{ '{:,}'.format(kpis.customers) }}</div>
  <div class="card"><b>AOV</b><br>${{ '{:,.2f}'.format(kpis.aov) }}</div>
  <div class="card"><b>Margin</b><br>{{ '{:.1f}%'.format(kpis.margin*100) }}</div>
</div>
<h2>Trends</h2><img src="{{ timeseries_img }}"/>
<h2>Breakdown by Category</h2><img src="{{ category_img }}"/>
<h2>Channel Mix</h2><img src="{{ channel_img }}"/>
<h2>Top Cities by Revenue</h2><img src="{{ geo_img }}"/>
<p style="color:#666;font-size:.9em">Generated by AI Report Factory.</p>
</body></html>
